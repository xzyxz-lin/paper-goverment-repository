# 结构化回收站功能说明

## 目标

将原先只有“已删除 ID 清单 / 一键全部恢复”的简化逻辑，升级为按项目、文件夹和论文分类的本地回收站。

## 本次实现

- 新增 SQLite 表 `recycle_item`，记录删除对象、所属账号、原项目和文件夹、删除时间、到期时间及关联内容。
- 删除项目、文件夹或论文后，会进入回收站并默认保留 7 天；文件夹记录会包含其子文件夹和论文。
- 回收站页面按“项目 → 删除项”分组，显示类型、原文件夹路径、删除时间与剩余天数。
- 支持点击选择、Ctrl / Cmd 增减选择、Shift 连续范围选择。
- 支持多选恢复；支持多选永久删除。永久删除会删除相应的数据库记录、笔记及回收记录关联的截图资产。
- 到期项会在服务启动、健康检查或打开回收站时自动永久清理。
- 旧版 `data/deleted.json` 中已有的删除项会自动迁移为新回收记录，保留原有可恢复状态。
- 回收站记录和论文列表均按登录账号过滤，避免不同账号看到彼此的回收记录或论文。

## 界面位置

- 项目总览顶栏新增“回收站”按钮和待处理数量角标。
- 左侧导航新增回收站入口和数量角标。
- 回收站中可按项目查看已删除的项目、文件夹与论文。

## 验证结果

- Python 后端与 JavaScript 前端语法检查通过。
- 本地服务重启后健康检查正常；未登录访问回收站接口返回 401。
- 旧删除清单已迁移：`PoppingC` 当前无旧回收记录，两个历史测试账号各保留 3 条已删论文记录，账号之间相互隔离。

## 数据说明

- 不会删除当前可见的项目、文件夹、论文或本地 PDF。
- 仅当用户在回收站点击“永久删除”，或回收记录超过 7 天时，才会清理对应的数据和截图资产。
