# 登录问题修复说明

## 发现的问题

1. 截图中的用户名 `PoppingC` 不存在于当前本地数据库，因此使用该用户名登录会返回“用户名或密码错误”。当前数据库中已有账号为 `t` 和 `t1`；密码未读取、未输出，也未修改。
2. 后端会话 token 保存在内存中，服务重启后旧 token 会失效。前端收到 401 后原先调用 `doLogout(false)`，而 `doLogout` 又会通过 `req` 请求 `/api/logout`；当该请求也返回 401 时，会重复触发同一流程，造成登录页面状态异常。

## 本次修改

- 修改 `web/library.js` 的 401 处理：直接清理内存和 `localStorage` 中的旧 token，并回到登录页。
- 修改退出逻辑：先清理本地登录态，再用普通 `fetch` 尝试通知后端退出；后端请求失败不再影响前端退出，也不会递归触发 401 处理。
- 修复 Shift 范围选择：记录每类列表的选择起点，按起点与目标项在当前列表中的索引取范围，支持上下两个方向；论文、项目、文件夹及复选框点击均适用。
- 未删除或覆盖任何项目、文件夹、论文、图片和账号数据。

## 登录方式

- 使用已有账号 `t` 或 `t1` 及其原密码登录；或点击“注册一个”，为 `PoppingC` 设置一个新密码。
- 服务重启后需要重新登录，这是当前后端采用内存会话 token 的设计行为。

## 验证

- 本地服务 `http://127.0.0.1:8040` 健康检查正常。
- 已确认前端登录页可加载，且数据库账号与截图中的 `PoppingC` 不一致。
- 代码修改后需刷新页面再验证旧 token 失效场景。
- 代码修改后已重新加载本地登录页面，页面正常运行且浏览器控制台无错误。

## 初步功能审计发现（尚未修改）

后端的大多数写接口虽然检查了登录 token，但部分接口只检查“已登录”，没有继续校验资源是否属于当前用户：项目树、论文查询、论文/文件夹/项目新增与修改、软删除、笔记操作等路径都需要继续补充项目归属校验。这个问题会影响“数据隔离”这一核心功能，建议下一步优先修复。
